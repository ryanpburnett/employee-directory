import React from "react";

const LocalEmployees = (props) => {
    return <div>
        <button onClick={props.localEmployees}>
            Local Employees
        </button> 
    </div>
}

export default LocalEmployees;