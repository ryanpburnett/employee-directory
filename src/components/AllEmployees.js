import React from "react";

const AllEmployees = (props) => {
    return <div>
        <button onClick={props.allEmployees}>
            All Employees
        </button> 
    </div>
}

export default AllEmployees;